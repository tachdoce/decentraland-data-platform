name: decentraland
version: "1.0.0"

profile: decentraland

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  decentraland:
    silver:
      +schema: silver
      +materialized: view
    gold:
      +schema: gold
      +materialized: table
